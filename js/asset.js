// js/asset.js
// --------------------------------------------------------
// 🏠 Asset App (จัดการหลักทรัพย์ค้ำประกัน)
// --------------------------------------------------------

(function (global) {
  "use strict";

  const AssetApp = {
    // ----------------------------------------------------
    // ⭐ คำนวณมูลค่ารวมของหลักทรัพย์ใน Lead
    // ----------------------------------------------------
    calculateTotalValue(leadForm) {
      if (!leadForm || !leadForm.contracts) return 0;

      // ตัวอย่าง Logic: วนลูปทุกสัญญา -> ทุกหลักทรัพย์ -> รวมราคาประเมิน
      // (ต้องมีการ Implement โครงสร้าง Asset ใน DataSpec เพิ่มเติมในอนาคต)
      return 0;
    },

    // ----------------------------------------------------
    // ⭐ ตรวจสอบความครบถ้วนของข้อมูลหลักทรัพย์
    // ----------------------------------------------------
    validate(assetData) {
      if (!assetData.type) return "กรุณาระบุประเภทหลักทรัพย์";
      if (!assetData.value) return "กรุณาระบุราคาประเมิน";
      return null; // ผ่าน
    },
  };

  global.AssetApp = AssetApp;
})(window);
