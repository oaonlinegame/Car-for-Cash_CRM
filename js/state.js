// state.js
// --------------------------------------------------------
// 📘 ไฟล์นี้เก็บสถานะ (State) ทั้งหมดของระบบ เช่น modal, pagination, และค่าการค้นหา
// --------------------------------------------------------

const AppState = {
  // ------------------------------------------------------
  // 🧭 UI states: ใช้ควบคุมการเปิด/ปิดเมนูหรือ modal ต่าง ๆ
  // ------------------------------------------------------
  isMenuOpenFilterSearch: Vue.ref(false), // สถานะเปิด/ปิดเมนูค้นหาขั้นสูง
  isOpenModalLead: Vue.ref(false), // สถานะเปิด/ปิด modal lead

  // ------------------------------------------------------
  // 🧠 ตัวแปรทั่วไปที่เกี่ยวกับการนำทางและแท็บ
  // ------------------------------------------------------
  searchRef: Vue.ref(null), // ตัวอ้างอิงไปยังช่องค้นหา (ใช้กับ v-menu)
  leadTab: Vue.ref("basic"), // แท็บที่เปิดอยู่ใน modal lead

  // ------------------------------------------------------
  // 🔍 Search state
  // ------------------------------------------------------
  searchQuery: Vue.ref(""), // ข้อความในช่องค้นหาหลัก

  // ------------------------------------------------------
  // 🎹 Hotkey system (กำหนดปุ่มลัด)
  // ------------------------------------------------------
  hotkeysEnabled: Vue.ref(true), // เปิด/ปิดระบบคีย์ลัด
  hotkeyMap: Vue.reactive({
    openBot: "Alt+B", // เปิดบอท
    openLead: "Alt+L", // เปิด modal lead
    closeAll: "Alt+C", // ปิดทุกเมนู
  }),

  // ------------------------------------------------------
  // 📄 Pagination (ระบบแบ่งหน้า)
  // ------------------------------------------------------
  page: Vue.ref(1), // หน้าปัจจุบัน
  itemsPerPage: Vue.ref(10), // จำนวนรายการต่อหน้า (หรือ 'All')
  totalPages: Vue.ref(1), // จำนวนหน้าทั้งหมด
  pagedLeads: Vue.ref([]), // รายการลีดในหน้าปัจจุบัน (computed)
};

// --------------------------------------------------------
// ✅ export ออกไปให้ไฟล์อื่นใช้งานได้
// --------------------------------------------------------
window.AppState = AppState;
